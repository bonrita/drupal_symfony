{% extends "base.html.twig" %}
{% import 'macros.html.twig' as macros %}

{% block title %}{{ 'Log in'|trans }} | BRT solutions{% endblock %}

{% block content %}
    <h1>{{ 'Log in'|trans }}</h1>

    {% include "components/tabs/log-in.html.twig" with {
        type: 'login'
    } %}


    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {{ form(form) }}

    {#{{ form_start(form) }}#}

    {#{{ form_row(form._username) }}#}
    {#{{ form_row(form._password) }}#}

    {#<button type="submit" class="btn btn-success">{{ 'Login'|trans }}<span class="fa fa-lock"></span></button>#}

    {#{{ form_end(form) }}#}


    {#<form action="{{ path('user_page_login', {'_locale': app.request.attributes.get('_locale')}) }}" method="post">#}
    {#<label for="username">Username:</label>#}
    {#<input type="text" id="username" name="_username" value="{{ last_username }}" />#}

    {#<label for="password">Password:</label>#}
    {#<input type="password" id="password" name="_password" />#}

    {##}
    {#If you want to control the URL the user#}
    {#is redirected to on success (more details below)#}
    {#<input type="hidden" name="_target_path" value="/account" />#}
    {##}

    {#<button type="submit">login</button>#}
    {#</form>#}

{% endblock %}